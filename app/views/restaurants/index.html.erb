<div class="jumbotron">
  <h2>En esta aplicación puedes encontrar los mejores restaurantes!!</h2>
  <p>Puedes realizar reseñas y calificar los restaurantes que conozcas, para que todos tus amigos las vean y también compartan sus opiniones.</p>
  <% if !user_signed_in? %>
    <p><%= button_to "Crea tu Cuenta Gratuita", new_user_registration_path, :method => :get, class:"btn btn-primary btn-lg" %></p>
  <% end %>
</div>

<p id="notice"><%= notice %></p>

<h1>Listing Restaurants</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <% if current_user.admin? %>
        <th colspan="3"></th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.name %></td>
        <td><%= restaurant.address %></td>
        <td><%= restaurant.phone %></td>
        <td><%= restaurant.website %></td>
        <td><%= restaurant.rate %></td>
        <td><%= link_to 'Show', restaurant, class:"btn btn-link" %></td>
        <% if current_user.admin? %>
          <td><%= link_to 'Edit', edit_restaurant_path(restaurant), class:"btn btn-link" %></td>
          <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-link" %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if current_user.admin? %>
  <%= link_to 'New Restaurant', new_restaurant_path, class:"btn btn-link" %>
<% end %>