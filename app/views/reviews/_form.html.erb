<%= form_for [@restaurant, @review], remote: true do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <label>Qué calificación le das?</label>
    <div id="star-rating"></div>
  </div>
  <div class="field form-group">
    <%= f.label :comment, "Por favor danos tús comentarios" %><br>
    <%= f.text_area :comment, class: "form-control" %>
  </div>
  <div class="actions form-group">
    <%= f.submit "Guardar",  class:"btn btn-primary"%>
  </div>
<% end %>

<script>
  $('#star-rating').raty({
    path: 'https://storage.googleapis.com/imagestipoyelp/raty/images/',
    scoreName: 'review[raitng]'
  });
</script>